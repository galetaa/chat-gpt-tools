{
  "name": "chat-gpt-tools",
  "version": "2.1.2",
  "private": true,
  "description": "Private, on-device ChatGPT performance and export tools for Safari and Chromium",
  "scripts": {
    "build:chromium": "node tools/build-chromium.mjs",
    "package:chromium": "./package-chromium.sh",
    "check": "npm run build:chromium && node tools/validate-extension.mjs && find Extension Build/ChromiumExtension -name '*.js' -type f -exec node --check {} \\;",
    "test": "node --test tests/*.test.js",
    "verify": "npm run check && npm test"
  }
}
