{
  "name": "chat-gpt-tools",
  "version": "2.0.2",
  "private": true,
  "description": "Private, on-device ChatGPT performance and export tools for Safari",
  "scripts": {
    "check": "node tools/validate-extension.mjs && find Extension -name '*.js' -type f -exec node --check {} \\;",
    "test": "node --test tests/*.test.js",
    "verify": "npm run check && npm test"
  }
}
